{
  "name": "final",
  "version": "1.0.0",
  "engines": {
    "node": "22.x" // Ensure your Node.js version is compatible with your dependencies
  },
  "scripts": {
    "start": "node server.js", // Start the server in production
    "dev": "nodemon server.js", // For local development (optional)
    "test": "echo 'No tests specified'", // Placeholder for tests
    "build": "echo 'No build step required'" // Placeholder for build step
  },
  "type": "module", // Enable ES modules
  "dependencies": {
    "@google/generative-ai": "^0.21.0", // Gemini API
    "axios": "^0.27.2", // HTTP client (if needed)
    "cors": "^2.8.5", // CORS middleware
    "dotenv": "^16.4.7", // Environment variables
    "express": "^4.21.2", // Server framework
    "mongodb": "^5.9.2", // MongoDB driver
    "mongoose": "^8.9.2", // MongoDB ODM
    "nodemailer": "^6.9.16" // Email sending
  },
  "devDependencies": {
    "nodemon": "^3.1.0" // Optional: For local development
  }
}
